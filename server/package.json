{
  "name": "server",
  "version": "1.0.0",
  "description": "Local server that exposes Codex usage from ~/.codex session logs",
  "license": "MIT",
  "main": "server.js",
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "lint": "node --check server.js",
    "test": "node --test",
    "start": "node --env-file=.env server.js"
  }
}
